· 长期记忆有了生命周期：显著性 = 置信度 × 时间衰减（半衰期 14 天）× 使用强化，替代固定排序；认知注入按显著性取前 24 条 / 1200 字符
· 自动归档：长期闲置且低信号的记忆自动软删除（退出注入与检索，可随时恢复）；再次被提及会自动复活
· 记忆支持 1~2 层分类路径（如 健康/训练），文件树落到「记忆/{类型}/{分类}/{主题}.md」
· 新增「AI 整理」（每天最多一次，可在设置关闭）：合并跨会话重复、统一分类、校准置信度、归档噪声、清理归档区
· 知识库页新增信噪比面板：注入覆盖率 / 低信号占比 / 归档数 / 最近维护时间，附「衰减维护」「AI 整理」入口
· 修复 Android 点击「安装并更新」闪退：R8 裁掉了 JNI 桥接类（补 keep 规则），并清理挂起的 JNI 异常防止崩溃
· 启动检查到新版本改为弹更新卡片（立即更新 / 稍后 / 跳过此版本），不再是一闪而过的提示
