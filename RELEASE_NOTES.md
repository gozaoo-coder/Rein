## v0.3.0

· **肌群模型从 26 键扩到 39 键，深层结构终于能高亮**。三视图里其实一直都画着 13 组深层结构（大圆肌、菱形肌、肩袖、前锯肌、髂腰肌、臀小肌、股中间肌、肩胛提肌、胫骨后肌、腓骨肌群、跖肌、腘肌、股方肌），但它们此前是「只会画、不能标」的灰底衬 —— 而动作要点里早就写着脸熟的名字（引体「主攻大圆肌」、坐姿绳索划船「菱形肌」、面拉「肩袖外旋肌」、悬垂举腿「下腹与髂腰肌」）。这一版把它们并进 `MUSCLE_KEYS`：`MUSCLE_LABELS` / `MUSCLE_DESCS` / 表单分组（按部位分八组）/ 容量地标四处同步补齐，AI 工具的 schema 也跟着变成 39 键。**深层键激活时会再叠画一层**（`.overlay`，0.72 不透明度）—— 否则它们画在浅层之下、被彻底盖住，又变成「标了看不见」。组件改成按「键是否属于 MUSCLE_KEYS」判定可高亮，不再看 SVG 里的 class：旧版本已生成的 SVG 与将来重建的都能跑

· **60 个内置动作的肌群表逐条体检、31 条修正**。修的是两类错：**漏**（拉类漏大圆肌/菱形肌、蹲类漏股中间肌、平板漏前锯肌与臀大肌、开合跳漏三角肌中束、跳绳漏前臂、仰卧起坐类的髂腰肌…）与**错**（上斜推类把三角肌前束也标成「主攻」——30° 上斜仍然是胸主导；「下斜俯卧撑」被规则按上胸处理；罗马尼亚硬拉与硬拉共用一张表，可 RDL 几乎没有股四头与内收肌负荷）。生成器加了硬约束：**每条动作必须在 SPEC 里显式写 `m`**（缺失即生成失败），并校验键与 1~3 档 —— 从此内置数据是评审过的显式表，正则只留给自建动作兜底。关键词规则表同步补上此前完全匹配不到的常见命名（开合跳 / 波比 / 高抬腿 / 登阶 / 壶铃摇摆 / 农夫行走 / 后踢腿 / 空中自行车 / 球类 / 拳击 / 爬绳战绳…），并把「手垫高俯卧撑」从「上斜」规则里拆出来（它是减负的下斜推视角，不是上胸动作）、给直立划船单列一张（中束+上束，不再按肩推处理）

· **工具与后端一起把肌群「管住」**。三个真问题：① AI 在课程条目上标好的肌群，按名自动建库时被写死成 `{}` —— 同一个动作在课程里有肌群图、在动作库里空白（还因为「显式表为空才回落规则」的分支差异，两边显示不一致）；现在 `ExerciseHint` 带上肌群并清洗后写进库记录。② Rust 与 mock 此前对肌群键**零校验**（未知键、档位 9 都能落库，前端抽屉就会渲染出 `undefined · 稳定`）；新增 `exercise_lib::muscles::sanitize_muscles` 与前端 `normalizeActivation()` 同语义（白名单 + 1~3 档），入库、入库前、种子三处都过一遍，并用种子里新写的 `muscleKeys` 元数据做**漂移单测**（生成器改了键、Rust 没跟上就红）。③ AI 工具补两个写入口：`upsert_exercise`（建/改自建动作，肌群为 39 键枚举）与 `set_exercise_muscles`（只改肌群，内置动作拒绝并回中文原因）；`list_exercises` 从「只回键名」改成回**完整档位表**，模型才有依据与库内动作对齐

· **动作库能筛、能收藏、能读要领、能看历史**（对照 wger 补的四块短板）。筛选面板：排序（最近使用 / 名称 / 练得最多）+ 类型 + 器材 + **肌群多选**（按部位分组，最多可选 39 块）+ 只看收藏，动作库页与课程编辑的动作选择器共用同一个面板；收藏是**用户态列**（迁移 0033，`favorite`），与 `hidden` 一样不被启动时的种子刷新覆盖，列表里星标一键切换、收藏永远置顶。**动作要领**（`steps`，同一迁移）：详情页渲染成分步编号列表，自建动作可在表单里逐行填写；内置动作的要领来自下面这条管线。**历史与 PR 面板**：近 10 次逐场（日期 / 组数 / 最重 / 容量）+ 三项个人纪录（最大重量、最佳估算 1RM、最佳单次容量 + 达成日期），口径与重量曲线一致（正式组，热身不计容量），1RM 复用建议引擎那套 Epley/Brzycki

· **接入 exercises-dataset（1,324 个动作 / MIT 数据）—— 但只取精选子集、且不碰媒体**。先做了取舍研究：数据集名称只有英文、肌群是粗粒度英文（`glutes`/`quads`/`delts`/`traps`，没有上/下胸、没有三角肌三束）、中文说明是机器翻译、媒体是 © Gym visual（仓库明确要求自行取得授权才能复用）。结论是**只把它当「候选动作池 + 中文要领素材」**：新增 `scripts/import-dataset.mjs` 转换管线（英文肌群词 → 39 键映射 `catalog/muscle-terms.mjs` + 人工精选表 `catalog/dataset-selection.mjs` + 评审草稿 `resources/_review/dataset-review.json`），首批导入 **21 个常用动作**（哑铃卧推 / 哑铃飞鸟 / 阿诺德推举 / 直立划船 / 钻石俯卧撑 / 器械推胸 / 反握引体向上 / 牧师凳弯举 / 潘德雷划船 / 壶铃摇摆 / 杠铃臀推 / 杠铃登阶 / 前蹲 / 相扑硬拉 / 高脚杯深蹲 / 坐姿髋外展 / 杠铃哈克深蹲 / 行走箭步蹲 / 俄罗斯转体 / 反向卷腹 / 农夫行走），动作库 60 → 81。**肌群表一律人工评审过的档位**（不采用数据集的粗粒度 target），中文名与要点自行撰写，只有分步说明取数据集的 zh 文本；媒体一张不进。来源与许可单独记在 `resources/exercises.SOURCE.md`

· **液态玻璃从「底栏那三块」铺到全部界面**。此前「超高」与「高画质」的差别只体现在底部 Dock 的三块玻璃与画质预览页的标本上 —— 其余所有 chrome（导航轨 / 信息栏 / 四个悬浮条 / 提示条 / 课表吸顶表头 / 页头圆钮）走的都是各自手写的 `--surface-translucent + blur`。这一版把材质收成**一份定义**：`.glass-surface` 叠到六层（半透明底 + 上亮下暗的受光描边 + 内顶高光 + 外投影 + **内圈细描边 + 上缘焦散 + 外缘层**），后三项是超高专属 —— 其余档位这几个令牌是透明的，所以**每一档画的层数一致、只是观感不同**（分层会让「标本」与真实界面各自漂移，这个项目已经付过一次代价）。超高的差异刻意**不来自加大 blur**：模糊半径是这条链上单个最贵的旋钮，大面板上成本随半径线性涨、而观感只是"更糊"；`html[data-perf='ultra']` 覆盖的是**整份 `--glass-*` 令牌**（底 0.52 → 0.42 更薄 + 三层光学细节），于是所有用这套令牌的表面一起升级，不必逐个组件去改。信息栏的问句条、导航轨、课表吸顶表头一并收编：贴边的用 `.glass-edge-*` 修饰只留靠内那一条描边（受光边是背景画的，收掉其余三边不影响它），吸顶表头是 8 个各挂一次 `backdrop-filter` 的格子、就地覆盖 `--glass-blur` 压到 14px —— 令牌化之后「同一套材质、不同成本预算」只需要覆盖一个变量。顺带把三处「暗场材质上下文」合成一个 `.stage-dark`（画质预览台的标本与参数面板的预览原本各抄一遍），并修掉提示条写死的 `rgba(250,250,250,.92)`：它在弱档下会变成一层发灰的薄膜，现在走令牌、弱档落成读得清的实底

· **折射划了预算：只给离散小控件**。`backdrop-filter: url()` 每挂一块都是一次独立的背景滤镜，成本随**面积 × 每帧重算次数**涨 —— 铺给谁不该看"哪个更显眼"，要看"面积够不够小"。名单定在：底部 Dock 三块 + 提示条（它的 `fill` 单独给 `--surface` 的 86% 而不是玻璃底 —— 提示条压在任意内容上、文字还是一行不换行的短句，底越薄越过一句就看不清一句）。大面积表面一律走材质升级：导航轨 / 信息栏 / 抽屉 / 会话面板不折射；页头圆钮用普通 `blur()`（38px 的圆上折射带只有一两像素、根本看不出来，还要压着页头那层 5 层的渐进模糊）。信息栏另有一条硬约束：它是一条可滚动的大面板，自己带 `backdrop-filter` 就会成为后代的 backdrop root，里面的问句条会退化成"只采到信息栏自己的内容"

· **动画丰富程度单独成一档**（设置 › 动效，与画质档位正交，存 `rein.motion.v1`）：`关闭 / 默认 / 丰富`。**默认就是此前那一套**，一个字节不改；`丰富` 才启用液态玻璃的动作；`关闭` 关掉全部补间。生效档位是**算出来的**、只有一个写入点（用户档位 → 系统「减弱动效」覆盖为关闭 → 掉帧降级压回默认，因为丰富档那几样都是每帧合成，正在掉帧时再叠上去是火上浇油），结论写 `<html data-motion>`；JS 侧的自绘动效（超范围回弹 / 悬浮条停靠弹簧 / AI 气泡入场 / 沉浸层形变 / 推理区展开）一律改读它，不再各自 `matchMedia`。**「关闭」关的是补间、不是状态**：`:active` 的颜色与缩放终值照常生效，只是瞬时到位 —— 否则界面会读成"失灵"

· **丰富档的四样动作**。① **液态活动底**（`GlassThumb`）：底栏药丸与分段控件的滑块变成一颗会融合的 blob —— 从按下的那一点长出一颗小球、与活动底连成一体，抬手分裂。融合是**涌现的**（`feGaussianBlur` + alpha 对比把两颗球之间的 alpha 斜坡推成一道实心的颈，不手写颈部几何）；位移走独立属性 `translate`、形变走 `scale` 的 keyframe（互不覆写，所以"加速拉宽压扁、减速收窄拉高"不会被位移打断）；浓度挂在**容器**上而不是 blob 上 —— goo 会把半透明输入推成实色，挂在 blob 上会让 0.62 的活动底变成纯白、比默认档还亮一档。分段控件那处换成「表面」语义：它坐在**不透明**轨道上，滑块要的是一块实白，不是压在玻璃上的半透明层。② **按压定向光晕**：触点位置写进 `--gx/--gy`，跟随手指的那圈高光**显隐交给 `:active`**（即时反馈不能等一个事件回来），落在底栏页签、分段控件与页头圆钮上；一份监听挂容器、按 selector 就近点亮那一格，按住横拖滑出哪一格就散掉。③ **透镜式进出**：抽屉 / 操作面板 / 菜单的遮罩从"一层黑影"换成"一层失焦"（`backdrop-filter` 的半径在 Chromium 里可插值，所以是渐变而不是硬切），菜单的 bind 面板从贴住的那个角**扩一个圆**（缩放会把面板里的文字一起压扁，圆形揭示只换"看得见多少"，内容从头到尾都是清的）。④ **滚动边缘**：向下滚页头缩一档、向上滚立刻还原（判定的是**方向**不是位置），只动 `scale` 不动字号与内边距 —— 滚动中改布局属性 = 整页重排 = 掉帧

· **每一档都有设计过的退化路径**：折射 → 普通毛玻璃 → 半透明实底 + 内描边；光学层在非超高自动透明；内核完全不认 `backdrop-filter` 时由 `@supports` 退成实底（而不是"一层发灰的膜"，此前这条路上没有任何处理）；「减弱透明度」退成实底 + 常规描边；`prefers-reduced-motion` 与应用内「关闭」同一条做法。另外修掉一处**静默失效**的过渡：`ExerciseVolumeCard` 的容量条写着 `transition: width var(--dur-slow) …`，而 `--dur-slow` 在全仓**从未定义** —— 未定义的 `var()` 会让整条声明在计算值阶段失效，等于它一直没有动画（数据一变直接跳）；现在补进令牌表（480ms，与底栏回弹、blob 位移同长），底座那处写死的 `480ms` 也换回同一个令牌

· **回归**：`scripts/e2e-perf-glass.mjs` 全绿（新增第 7 节「动效三档」：关闭档每条过渡都被压到 0.01ms、不渲染活动底；默认档补间照常且活动底仍在纯色那一层；丰富档标本与**真实 Dock** 都渲染 blob、纯色底让位、切回默认又回来。新增第 8 节「超高材质」：逐档读令牌 —— 高画质时光学层透明、超高时三项都有值且 blur 与高画质**完全相同**、弱档归零且模糊为 none。第 1 / 6 节的「折射底与高画质同色」按新契约改写为「三块都取自当档生效的 `--glass-fill`」并加一条「超高的底比高画质更薄」）；`scripts/e2e-muscle-map.mjs` 18 项（39 分区齐备 / 深层键激活后叠画可见 / 单束高亮不牵连相邻束）；`scripts/e2e-exercise-library.mjs` 47 项（新增筛选面板、收藏置顶与落库、要领分步列表、历史与 PR 面板、导入动作的肌群评审结果）；`scripts/e2e-strength.mjs` 27 项（训练会话无回归）。`cargo test` 361 项（新增肌群白名单、要领清洗、种子刷新不覆盖用户态列、自动建库带肌群四组）；`npm run contract` 三方命令 210 条一致；`npm run build` / `npm run lint` 干净

· **本轮未做**：超级组（课程里的 A1/A2 交替做组）。它要把会话状态机从「动作 × 组」的线性游标改成**按轮推进的槽位队列**（现游标模型假定「游标之前的格子非完成即跳过」，交替做组会破坏这条不变量），风险集中且需要一个专属 e2e 兜底，留到下一版单独做 —— 数据侧（`PlanExercise.group`）与详情页徽章已经在位

## v0.2.13

· **底部 Dock 从「一条玻璃」改成「三块玻璃」**：左独立圆钮（**可自定义落点**，默认课表；长按弹出选择器换一个模块主页面，候选来自插件层 —— 关掉某个模块它自动从清单里消失、存量落点回落到第一项）+ 中药丸（内核两格「主页 / 我」+ 插件条目，如运动）+ 右独立圆钮（AI）。三块**并列留缝、互不叠压**：各自都带受光边，一叠就会在缝上出现一道月牙形的硬边（与标本台同一条结论）。为什么拆：AI 与课表这类模块主页面都值得一键直达，而「主页 / 运动 / 我」三格又必须留在最顺手的位置 —— 于是最左最右两个位置做成独立圆钮，各放一个高价值落点
· **Dock 的尺寸关系与前景只有一份定义**（`base.css` 的 `.dock-block` / `.dock-tab` / `.dock-slot`，含受光亮斑与按压弹簧），**画质预览页的底栏标本直接用同一份** —— 标本的意义就是「所见即应用里的那一块」。此前标本套的是暗场令牌 + 自己的一版页签（平色亮斑、`--hero-text-dim` 前景、图标钮不带标签），与真实 Dock 长得不像。回归里逐项比对两边的**计算样式与几何**（块数 / 页签前景 / 受光亮斑 / 玻璃底色 / 折射挂载），亮暗两色 + 弱档 + 减弱透明度各比一次
· **液态玻璃参数可调**（画质预览 › 液态玻璃参数调节）：管线上 11 个数字（边缘厚度 / 中心亮度 / 贴图不透明度 / 贴图模糊 / 折射位移强度 / 三通道偏移 / 边缘柔化 / 背景饱和度 / 玻璃底色浓度）摊成滑杆 + 可点输入的数值 —— **英文键名 + 中文名**并列，区间给宽（这一页是给「调到合适为止」用的，不是日常微调）。面板自带一块暗场预览（圆 + 药丸 + 圆，按真实 Dock 的装配摆），改动**即时生效**（滤镜定义跟着重烘）并存本机 `rein.glass.v1`，「恢复默认」回到出厂那组。**这一版的出厂值就是在这块面板上定稿的**：边缘厚度 0.28 · 中心亮度 56 · 贴图模糊 34 · 位移强度 −31 · 边缘柔化 1.3 · 背景饱和度 1.4 · 底色浓度 0.46
· GlassSurface 的材质参数默认值外置到 `system/glassParams`（调用处仍然只给尺寸与圆角），并新增 `fill` prop：折射分支的底色可以直接给颜色 —— **Dock 传 `--glass-fill`**。此前折射底固定是半透明黑 / 白，暗色下那层纯黑把页签文字的真图对比度压到 2.52（高画质档同一位置 2.72）；现在两档同色，「开了折射」不再意味着对比度回退（回归把「同色」钉成结构不变量，而不是靠阈值兜）
· **数字步进器上那个挡住读数的圆圈修掉了**：数值钮与两侧 +/− 钮同在 `.ctrl` 里，被同一条 `::before` 规则盖上了一个 30px 的圆底 —— 圆正好落在数字上、颜色还与两侧步进钮一致（`--surface-2`），把值挡得看不见。规则收窄到 `:not(.val-btn)`，数值钮改回自己的 44px 命中区、只在自己被按下时铺一层浅底
· **抢课浮条的读屏播报**：可见那行里嵌着每秒都在变的倒计时，而它被挂成了 `role="status"` 的 `aria-label` —— 一个**每秒改写自己名字的 live region**：读屏要么被刷屏、要么排出一长串已经过时的播报，用户还没有办法让它停下；这条浮条又是全应用常驻的，等于每翻一页都带着它。现在拆开：可见内容 `aria-hidden`，播报另走一条**只在跨档时才改写**的隐藏文本
· **抢课面板里最危险的那一格**：「**窗口已经开了，而任务单是空的**」—— 名额正在以秒为单位消失，而他手上一条任务都没有。它此前长得却是一天里最平静的样子（没有倒计时、没有变色、没有一句话说要紧），恰恰把更危险的态画成了更安心的态。现在这个态自己站出来：换底色、把窗口时间从副信息里提出来、明确说清「现在该干什么」（判据用 `allowEnter` 而不是「有没有批次」）。同时把「窗口开着」拆成**事实**（说出来 + 标色）与**警报**（进了批次却一条任务都没排才整块变色）—— 正选窗口可以开好几天，一直报警就成了狼来了。起飞前自检的结果**先看坏的**且不截断：自检是用户自己点出来的，他要的就是一份完整结论
· **选课页：上课时间地点直接出现在列表上**（一门课挂 7 个教学班时，「谁在什么时候上」才是选班的依据，藏在抽屉里等于逼用户逐个点开背下来；教务把 `dateTimePlace` 给成字符串 / 数组 / 嵌套对象都见过，按「能认出来就认」处理，与 Rust 匹配器同一套读法）；**已选中的课也能打开**看设置（此前对已选直接 return，抢到手之后这门课就变成一块点不动的死砖，「我当时选的是哪个组」全应用没有任何地方答得上来）；名单**新鲜度**写成「N 秒前拉的名单」（座位数是会变的，一份 90 秒前和一份 3 秒前的名单看起来是同一行字，但前者可能已经没位了）；余量紧挨课名（60 / 60 与 118 / 120 是这一行里最该被比出来的量）；「已选」从蓝底白字的行动变成淡蓝底的既成事实；批次公告（教务原话里常有「只能选 X 门」「先到先得」这类硬约束）
· **开课查询那张永远卡住的卡片**：组件自己存了一个 `semesterId`，但全文件没有任何地方给它赋过值（`if (!semesterId.value && …)` 写在「它非空」的分支里，是够不着的死代码），于是它永远停在「还没有拿到学期列表」，而它给的出口「先刷新一次」只会再弹同一句话 —— 用户被卡在一个没有出路的循环里。学期改从校园 store 取（唯一来源）；顺带修掉两个**根本不存在的令牌**（`--fs-title` / `--text`，阶梯里叫 title1/2/3、text-1/2/3），并改成先探一次两个域再查第一页、把结论翻译成学生能处理的话（域名 / EAMS5 / HTTP 码是他没法处理的东西），技术详情折起来但拿得到
· **命中区与焦点**：抢课面板那一排 16px 高的文字按钮、抽屉唯一一扇「什么都不选就退出去」的门、页头返回键与图标钮 —— 视觉尺寸不动，命中区一律撑到 44×44；抽屉的关闭键此前 `outline: none` 之后没有任何替代焦点环，补上；抽屉的固定操作区不滚不缩、永远钉在底部
· **抢课结果的措辞**：教务最近那句话**如果只是在复述徽标，就不必再说一遍**（抢到之后 `lastMessage` 常常就是「已抢到」，而左边那枚徽标已经写着同样的三个字 —— 真正的新信息是上面那行「已尝试 N 次」）；「你连的是正式教务」这条警告不再拿 `--warn` 直接当文字色
· 回归：`scripts/e2e-perf-glass.mjs` 重写第 1 / 6 节（**标本与真实 Dock 逐项一致** + 三块玻璃逐块折射 / 与高画质档同色 / 不参与布局）并新增第 7 节参数面板（11 行参数、拖滑杆滤镜跟着重烘、点值就地输入、恢复默认）；`e2e-feature-toggles.mjs` 补底栏三块与左钮长按换落点（含刷新后持久化、关掉模块后回落）；`e2e-course-select.mjs` / `e2e-grab-plan.mjs` 跟着本轮改造更新。布局守卫 27 个路由仍全绿

## v0.2.12

· **液态玻璃从「标本台」落到真实界面**。上一版的超高档折射只活在画质预览页里，这一版把它装到会天天看见的地方：底部 Dock 在超高档垫一层 GlassSurface 折射 —— 与预览页的标本**同一个组件、同一组默认参数**，只有底的浓度按真实内容调厚（0.5 ≈ 高画质档 `--glass-fill` 的 0.52）：标本压在暗场壁上、前景永远是浅色，真实 Dock 背后却是任意页面，底再薄一点，页签文字的真图对比度就从 2.42 掉到 2.04（这条现在是断言，不是估计）。折射层必须垫在**最底层**——采样的是它背后页面真实内容，所以这一档 nav 自己不再画底、不再模糊，否则采到的是自己那层色膜
· **受光边不许丢**：nav 保留 border-box 的渐变描边（上亮下暗），折射层 inset 1px 正好让出这条边。玻璃的「厚度」全在这条边与内顶高光上——换成组件就把它们丢掉的话，超高档会比高画质更像一块塑料膜
· 新增全局材质类 `.glass-surface`（`styles/base.css`）：fill + 受光边 + 内顶高光 + blur(28) + 影，**Dock 与四个悬浮条第一次用同一份定义**。此前悬浮条走的是 `--surface-translucent` 的「毛玻璃」（无受光边、无内顶高光），与 Dock 的玻璃根本不是同一种材质——GlassSurface 的注释里写着「与 Dock / 悬浮条同一套材质」，那句在本次之前是假的
· 顺手修两个 GlassSurface 的真缺陷：① `style` prop 声明了却**从未接线**（文档承诺「与内部尺寸/背景合并，同名以调用方为准」，实际只有内部样式生效）——折射层因此参与 flex 布局、把四个页签挤到右边、还把固定定位的 Dock 撑出横向溢出；现在接线，几何（定位/层叠/命中区）由调用方说了算；② 圆角解析只认数字与 px 字面量，`var(--radius-full)` 会被解析成 0（位移贴图的圆角全错）——改成优先读**计算值**，CSS 变量 / 百分比 / calc 都能落到像素
· **回归锁死**（`scripts/e2e-perf-glass.mjs` 新增「真实 Dock」一节，9 条）：超高档挂 `url(#glass-filter-*)` 且 nav 不画底（只剩 1 层描边渐变）；受光边在（`background-origin: border-box`）；折射层不参与布局（不撑出横向溢出）；页签对比度**真图采像素**不回退（亮色 超高 2.40 vs 高画质 2.42；暗色 2.72 vs 2.62；基线锁定 ≥2.0，弱档/减弱透明度两档的 6.2+ 照旧）；弱档不挂层也不模糊。实测 A/B：超高＝能看见背后卡片被软化/折射的薄玻璃，高画质＝毛玻璃白条，差异一眼可辨
· **错误码贯通教务自愈**：`ReinError` 跨 IPC 从裸字符串改为结构化 `{ code, message }`，教务「会话失效」的五个产出点统一打 `session_lost`；`transport.ts` 把它归一成 `IpcError`（Error 子类，带 `code`）——`e.message` / `String(e)` 的老写法一字不变，需要分派时看码。后端 `is_session_lost` 与前端判据都改成 **code 优先、关键词兜底**：判错文案意味着「不会自动重登」，那是这条链路上代价最大的误判
· **联网工具的 SSRF 加固**（`web_fetch` / `web_search` / 教务救援面）：① 逐跳跟重定向，每一跳都重新过白名单（此前 ureq 自动跟随只校验首地址，一条公网 URL 用 302 就能把我们带进内网）；② 域名解析出的每一个地址都要过（IPv4 补 CGNAT / 保留段，IPv6 补 `::ffff:` 映射、`64:ff9b::` NAT64、链路本地 / 站点本地、多播）；③ **修掉一个真实绕过**：IPv6 字面量 `http://[::1]/` 此前直接放行（`host_str()` 带方括号，两种解析双双落空）；④ 解析失败改为拒绝（fail closed）；⑤ `campus_http` 的公网分支不再跟随重定向——探针要看到的就是 3xx 本身
· `web_fetch` 工具此前拿不到 content type：Rust 与 mock 发出的键都是 `contentType`（`rename_all = camelCase`），而前端声明的、读的都是 `content_type` —— 两种模式下这个字段恒为 `undefined`（手抄类型的镜像里，这类漂移只有真机才暴露）。已两端对齐
· **知识库索引进度改成事件推送**：worker 每推进一轮 emit `kb://index`（此前**发了没人收**），`kbService.onKbIndexProgress` 订阅、知识库页就地更新状态条——在页面里等索引时不再只有进页面那一瞬间的快照
· **CSP 白名单式落地**（`tauri.conf.json`）：`default-src 'self'`，asset / data / blob 按需开口，`devCsp` 放行 Vite HMR；Tauri 会为自己的注入脚本自动补 nonce。自定义命令不受 capability 约束，所以这是这条边界上唯一的硬闸门。**已在真机（WebView2）验证**：构建版启动后主页完整渲染、数据经 invoke 正常取回
· **命令契约进 CI**：新增 `npm run contract`（解析 `lib.rs generate_handler!` × `services/*.ts` 的 invoke × `mock/server.ts` 的 case，三方集合必须完全相等）与 `npm run lint`（ESLint 扁平配置，只收正确性规则、不碰排版），CI 每个 PR 跑；顺手清掉 mock 里 `Number(x) ?? 1` 这类**会产出 NaN 克重**的写法、抢课设置 `leadMs` 的 NaN 路径
· 待办 / 营养两个域补上首批单测（`sync_recurrences` 的窗口物化幂等、改规则后未来实例自动收敛、已完成行不删；营养汇总按克重缩放与按日过滤）；`app_meta` 的读写收拢到 `db` 唯一一份实现（此前 5 个模块各抄一遍 SQL）
· `scripts/e2e-wbar-drag.mjs` 补上 `REIN_E2E_URL`（与其它 33 个 e2e 同一条约定）：本机 Windows 把 1420 划进了保留端口段时，这条链路的回归不再整条跑不起来
· **抢课引擎把「抢课」项目里那些会真出事的东西移植过来了**（完整比较与移植清单见 `docs/campus-grab-vs-qiangke.md`）。最要命的一条是**字段类型**：教务用 Jackson 强类型接 `studentAssoc` / `courseSelectTurnAssoc` / `lessonAssoc`，我们此前发的是字符串 ⇒ **每一次提交都是 500**（真机原文 `no String-argument constructor/factory method … String value ('1921')`），现在发数字。其次是**占位回执要逐条判词**：`success:true` 只说明单子处理完了，`result.{lessonId}.textZh`（`ATTEND`）才是判过没判过 —— 判死的课再发正式请求是纯浪费。时钟改成**多次采样取区间交集**（旧的单次采样 + 整秒截断会系统性偏小 0~360ms，真机实测交集口径 ±88~216ms）；解析失败（`JSON parse error` / `HttpMessageNotReadable`）归我们自己按 `BadRequest` 停下交 AI，不再当「教务崩了继续打」；`HttpMessageNotReadable` 那类硬条件（`不符合选课条件组要求`、`相同教学班只能选一次`）判终态；错误正文（200 字）带上，正是它让 500 的真因当场定位
· **抢课的能力补齐（「抢课」有而我们没有的那几样）**：座位数改走 `std-count` 批量接口 —— 该部署的 `query-lesson` **根本不回** `stdCount`（2026-09-22 实测某批次 448/448 缺失），「有空位的先出手」此前等于没有，现在补完名额还会**重排**（满员的班沉到后面）；匹配加了**放宽阶梯**（严格 → 丢名单里查不到的词 → 丢太笼统的时间地点词），三条红线都锁在测试里 —— 严格档有命中就绝不放宽、像人名与课程代码形状的词永不丢、丢完还得认得出是哪门课（`量子力学 张伟` 丢掉课程名就会去抢张伟的**别的课**，这条是端到端测试逼出来的）；被拒之后按 `REJECT_COOLDOWN_MS`（1 秒，且必须长于节流间隔）**就地换人**，窗口内满员改为按节流节奏重投（默认 700ms，不再是每 5 秒才看一眼名额池）；受理单落 `request_domain` 列（迁移 0032）—— 两个域是两套系统，受理号**不能跨域查询**，镜像域的单子只能问镜像域（此前会被当成主域的号一路空转到上限再重投，真结果就在镜像域上没人看）；教务拉不到名单时用**落盘的那份**接着干，并在界面上标明「这是旧名单」；新增**起飞前自检**（`campus_grab_preflight`，登录 / 令牌 / 批次 / 时钟 / 名单 / 每条计划逐项 GO·NO-GO + 证据一句话，按需触发）
· **抢课面板**跟着补上四件事：名额显示（「已选 / 上限 · 余 N」，此前这一栏一直是空的）、起飞前自检的一行按钮 + 逐项结果、放宽时明说「已放宽：忽略了…」并点出被丢掉的词、任务行补「X 秒后重试 / 马上重试」（被拒后的冷却不再看起来像卡死）。回归：五个浏览器 e2e 共 126 项（auto-grab / plan / squads / emergency / course-select，其中新增 8 项专测放宽与体检），Rust 单测 346 项
· **自绘超范围回弹**（`src/system/rubberScroll.ts`）：Android 12+ 把系统级边缘效果从 glow 改成了 stretch，WebView 里表现为到边拖动整页拉伸 —— 原生层已关掉（MainActivity `overScrollMode = NEVER`），这里在 Web 层自绘替代：到边继续拖 = 超范围平移、位移走 `f(x) = k·ln(1+x/k)`（`f(0)=0`、`f'(0)=1`，因此与原生滚动的交接没有速度突变；`x ≫ k` 之后渐近对数，越拖越沉），松手弹簧回位（与悬浮条拖拽同一组参数）。判定按「可滚动祖先链 + 链式语义」逐 move 增量累加：`overscroll-behavior: contain` 处截断、`touch-action:none` 的子树（浮条拖拽 / 地图 / 待办芯片）整个手势放弃、输入控件不接管、反向拖回 0 立即交还原生。全应用只挂一份 window 监听，三类层用 `data-rubber-page|content|self` 就地标注（页面层 / 容器内容层 / 横向条），抽屉、弹层、周时间线、AI 消息区一并接上。回归 `scripts/e2e-rubber-scroll.mjs` 11 项（曲线逐点、轴锁、交接无跳变、幽灵点击、内层容器与横向条）
· **更新说明只显示「比本机高」的段落**（`src/utils/updateNotes.ts`）：更新清单里的 notes 是**整份 RELEASE_NOTES.md**（`scripts/release/publish.mjs` 原样读整篇塞进去的），于是每次更新都把 v0.2.1 一路铺到最新版，真正要看的「这次改了什么」被埋在最上面那段之外；现在按 `## vX.Y.Z` 切段只留高于本机的段。两处取舍都是「宁可多显示，不可不显示」：解析不出版本标题、或本机版本不可解析 → **原样返回**（说明是外部输入，不能因为换了格式就变成空白）
· **训练沉浸页的「本组登记卡」**（`components/exercise/SessionOverlay.vue`）：重量与次数是同一组记录的两个字段，此前分在两处（重量在卡里、次数裸在卡外当大字），视觉上像两件不相干的事、中间还夹着今日状态那张卡；现在同卡同构 —— 标签定宽在左、± 分列两端、数值居中，两行因此左右严格对齐。「建议 / 上次 / 计划」三个出处**等宽并列**成一排可点填入（主推的那个用卡片底的浮起态，不用主色实底：它是「可以填进去」的候选，不是已经生效的状态），今日状态自评收进同一张卡的上分段（它正是下面那条建议的输入，先说因后说果），建议依据展开时从第二条列起、不再重复摘要那句。步进钮从两枚裸字形（`−` 是 U+2212、`＋` 是全角，两者字宽差一档、左右不对称，落在卡片空白里也不像按钮）换成**看得见圆底的 svg 图标**，数值行改成撑满容器、两钮落在两端（步进距离够拇指），`size` 档位收掉
· **动作的肌肉激活映射逐条校正**（`config/muscles.ts` + `scripts/gen-exercises.mjs` 的逐动作 `m` 覆盖）：规则表按「专项先于泛规则」重排，补上此前整套漏掉的肌群 —— 拉类（引体 / 下拉 / 划船）的后束与斜方中下束、握力；跑 / 走 / 跳绳 / 爬坡的**胫骨前肌**（踝背屈）与**臀中肌**（髋外展稳定）；蹲类的臀中肌与内收肌；臀推 / 臀冲 / 臀桥从硬拉模板里拆出来（臀主导的髋伸，不把竖脊肌按主攻标）；挺身 / 山羊单列（竖脊肌才是主攻）；屈膝位提踵给比目鱼肌、侧平板 / 侧桥给腹斜肌、脚垫高的俯卧撑给上胸、双杠 / 凳上撑体从纯肘伸里分出肩伸参与；正面平举补上胸。动作库里没有现成规则覆盖的（单腿臀桥、靠墙静蹲、深蹲跳、开合跳、波比跳、高抬腿、卷腹、划船机…）用逐动作覆盖钉死
· **三处布局修正**：AI 页改成「整帧」（页面盒恒为 100dvh、安全区不再从高度里扣，改由页头与输入栏各自让开 —— 从前高度一缩，页头与底栏就被挤进「视口减安全区」的盒子里，与别处观感不同）；页头的粘滞位**不能**写成滚动容器的 padding-top（sticky 的粘滞位会**叠加**在容器的上内边距之上：实测 `.msgs` `padding-top: 52` + `--ph-stick: 42` 时页头落在 94，被顶下去一整段），挪到页头自己的 margin 上；记账 / 食物库 / 模型三个页面的悬浮按钮 Teleport 出页面层（超范围平移层是 translate 元素，会改 `fixed` 后代的包含块，留在层内拖动时会跑位）

## v0.2.11

· **画质预览页（设置 › 性能）重做**。液态玻璃折射的是**它背后的东西**，所以这一版把预览台改成**暗场**：壁纸换成应用自己的暗场材料（与跑步页、训练详情同一套 `--hero-*`），一道柔光带 + 同色相族的两团光晕（运动绿 → 均衡青），只留一层 1px 细格当量尺。上一版拿五段满饱和撞色 + 30% 白斜纹当"高频参照"，结果玻璃边缘那道色散被背景的噪声淹了 —— 现在高频只留一处、降到 3.5% 白，位移错开几像素一眼可见
· 台上三件改成**三层陈列**、整行铺满：读数胶囊如实说当前档位（折射已开启 / 本机内核不支持 / 已降级到流畅优先），两块基本件（正圆 54 × 54、胶囊 156 × 54）与整条底栏（高 58）各占一行；不再是把三块玻璃漂在中间、与底栏之间留一条没有意义的空档
· **页签文字不再随主题碰运气**：玻璃压在暗底上一律走深色（`tint="dark"`），前景固定浅色；选中态由**中性白亮斑 + 前景明度**承担（不带色相 —— 带色相的衬底压在任意壁纸上会与背后的内容混成脏色）。对比度这次是**在真图上采像素**量的，不是算合成：未选中页签 **6.08:1**、选中衬底 **8.59:1**、读数胶囊 **14.11:1**（上一版亮色模式下合成约 1.4:1，那个页签是真的读不出来）
· 退化路径同样如实：内核不支持 `backdrop-filter: url()`（Safari / Firefox）时明说「已退化为普通毛玻璃」并且不挂滤镜；弱档与系统「减弱透明度」下玻璃顶成实底、受光亮斑归零、折射一并关掉（折射底与滤镜是内联样式，媒体查询够不着，这一版从调用处压住），前景仍读得出来（实测 6.26:1 / 6.25:1）
· 新增 `scripts/e2e-perf-glass.mjs`：画质预览页的**像素级**核验 —— 亮/暗两套主题下采真图算页签与胶囊的对比度、拖动态、三种退化态、`prefers-reduced-transparency`、320 窄屏不溢出不挤
· **手写超范围手感**：关掉 Android WebView 的原生下拉拉伸（整页 scaleY），改由 `system/rubberScroll` 自绘 —— 从触摸目标向上找可滚动祖先，累计位移超过 4px 锁轴，之后按 `x ≫ k 渐近 log_B(x)` 的对数阻尼平移，越拖越拖不动（起点速率与位移 1:1，之后阻力渐增）。全应用只挂一份 window 监听，容器增删 / 弹层动态挂载自动适配；页面层、横向条（含画质预览的壁纸）与纯文本盒各挂一个属性即可。`scripts/e2e-rubber-scroll.mjs` 锁住这套手感
· 修正：**页面层拖动时悬浮按钮会跟着跑位** —— 页面层被 translate 之后成了 fixed 后代的包含块，管理模型页的「添加模型」FAB 于是随页面一起晃。改为 Teleport 到 body，与页面层解耦
· 修正：**部分页面滚起来页头贴不住容器顶** —— 页头的滚动容器带着 padding-top，而 sticky 的粘滞位是从容器「内容盒顶」起算的，页头会被顶下去「内边距 + `--ph-stick`」那么多（真机 safe-top=42 下实测落在 94，页头上方留出一条 52px 的空白，那条里的内容没被遮住地从页头上方滑过去）。上内边距挪到页头自己的 margin 上：静态位置一模一样，滚起来一路贴到容器顶
· 修正：**做组页的数字步进器**两侧是两枚裸字形（「−」是 U+2212、「＋」是全角），落在卡片空白里既不像按钮、两者字宽还差一档、左右不对称。改成看得见的圆底图标按钮（svg），整行撑满、数值居中
· **更新说明按本机版本裁剪**：清单里装的是整份 `RELEASE_NOTES.md`，以前会把早就装过的版本段落一起显示，现在只显示比当前版本高的段落
· 抢课：移植抢课脚本里更成熟的三处 —— **时钟校准**改多采样取区间交集（旧实现单次采样 + 整秒截断，系统性偏小 0~1s；对方同一套算法实测 ±0.084s）、**占位预检**（名单里已判死的班不再发正式请求，不再白耗窗口里的出手机会）、**双域受理单归属**（镜像域的受理号不再拿去问主域）；被拒之后在窗口内加速重排。逐维度对比与移植清单见 `docs/campus-grab-vs-qiangke.md`

## v0.2.10

· 起因：有反馈「管理模型卡片内、软件更新的行间、AI 页输入栏与底栏之间」间距异常。**逐处实测核对后，这三处布局都是对的** —— 用 adb 连上真机（Xiaomi 24069RA21C / Android 16 / WebView 150 / 406×904 CSS 视口 / dpr 3）直接读 WebView 的 DOM 量的，不是看截图猜的：模型卡片「名称 → provider·modelId」= **3px**、「→ 价格」= 4px、「→ 能力徽章」= 10px；软件更新「更新设置」**行与行之间 = 0px**（各行自带内边距，行高 58~81px）；AI 页**输入栏底 = 底栏顶（0 间隙）**、输入栏 56px、底栏 58px —— 与 CSS 声明逐条一致。同一批数字在改前改后也一致（用 `HEAD~1` 的旧版重跑过），所以这三处不是某次改动引入的
· 那批截图的样子（空档、控件被切掉一角）与「稳定态量不出来、离开页面就自愈」的特征吻合**瞬态重排/重绘帧**：logcat 里能看到当时正反复发生 display state 切换与 onConfigurationChanged 重配置。**若再遇到，请连同当时的操作一起反馈** —— 现在有工具可以直接抓现场（见下）
· 顺手扫出并修掉一个**真毛病**：中文短标签会被拆行 —— 真机 406px 视口下「火山方舟 · deepseek-v4.1-flash **在线**」的徽章被拆成「在 / 线」两行，卡片高度跟着变，看着就像布局错乱。徽章/标签（chip / badge / cap / pchip 等）统一不换行：真放不下时宁可溢出，也不拆字
· 新增 `scripts/e2e-layout-guard.mjs`：**布局守卫**，26 个路由 × 三类断言 —— 徽章拆行（`getClientRects().length > 1`）、真实裁切（按**内边距盒**算，排除有意的省略号与横向滚动条带、缩略预览）、以及上面那三处**间距不变量**。为什么不能只靠截图：空元素没有墨迹，像素扫描量不出它占的高度，而这正是「莫名空档」最典型的来源。跑一次 `node scripts/e2e-layout-guard.mjs` 即知有没有漂
· 新增 `scripts/diag-android-layout.mjs`：**真机布局体检**（adb → WebView DevTools → 直接在设备页面上求值）。能读 `--safe-*` 的**解析值**（`getPropertyValue` 拿到的是未解析 token，`parseFloat` 会得 0，这个坑写在脚本头里）、扫「看不见却占高度的空盒子」、按需把每个盒子用 outline 画出来（`--border`，outline 不参与布局，不影响被观察的几何）。release 包默认关着 WebView 调试，需要时在 MainActivity 里临时打开再构建安装（同签名覆盖，数据不丢）

## v0.2.9

· 修正：**渐进式模糊在高画质下根本画不出来** —— 上一版把「升级后消失」归到了画质档位的缺省值上，那只是把遮罩重新挂上去；真正的病根在遮罩自身：渐变包络挂在**容器**上，而带 `mask` 的元素会成为 `backdrop-filter` 的 backdrop root，子层于是只能在容器**内部**采样 —— 容器里除这几层半透明的东西空无一物，整块模糊最终渲染成「什么都没有」。表现极具欺骗性：DOM 里 5 层俱在、`backdrop-filter` 与 `mask` 的计算样式全部正确，屏幕上一片干净（所以只看计算样式的那批断言全绿，问题照样发出去）
· 修法：**包络折进最弱那一层的梯度里**（它本来就铺满整段，让它从 55% 一路淡到远端），全程只有「一层一个 mask」—— 数学上与原「层梯度 × 容器包络」相差不超过 0.1 的 alpha。同一个坑的两个变体也一并试过并弃用：给容器加 mask、给层加多重 mask 求交（`mask-composite: intersect`），实测都会让该层失去 `backdrop-filter`；这些结论写进了组件文件头，免得后人再踩
· 顺带修掉一个**假淡入**：滚动显形与切档淡入的 opacity 过渡原来挂在容器/组件根上，而容器 opacity<1 同样是 backdrop root —— 那 0.2 秒里模糊压根不渲染，结束才「啪」地弹出来，看着像闪现而不是渐显。现在过渡挂在**层自己**身上，淡入是真的淡入（降级档的底色渐变不受此限，照旧用容器淡入）
· **回归锁死为「量像素」**：页头区新增像素级断言 —— 在页头背后铺一张高频条纹，糊过之后条纹并成灰、边缘能量骤降；未滚动时同一段条纹必须仍然清晰（反证判据不是恒真）。这类「样式正确、像素为空」的故障，计算样式断言天生抓不到。已实测这条断言的有效性：把容器 mask 加回去，它立刻变红，而其余 35 条照旧全绿
· **全页面标题区走查**：新增 `scripts/shots-page-headers.mjs`，逐个带页头的路由截图并量 safe area 与遮罩几何。无头浏览器的 `env(safe-area-inset-*)` 恒为 0，脚本按真机注入 `--safe-*-native`（与 Android 原生侧同一条通路），才验得到「页头压进状态栏」这类只在真机上出现的毛病：27 个路由在移动壳下全部通过（页头贴住 safe-top、遮罩盖住状态栏那条并铺满整帧），桌面三窗格下同样逐个核过

## v0.2.8

· **全校开课查询**：新增一整块能力 —— 看清全校开了哪些课、谁在什么时候上、在哪个校区。它与**选课批次无关**，批次还没公布的时候照样能查，这正是「提前规划抢什么」的依据：先看清开课名单与时间地点，再去排志愿。入口在选课页（批次列表下方）；窗口没开时它就是那里唯一能做的事
· 开课名单能按**课名 / 课程代码 / 教学班名 / 教师 / 上课时间地点**随手筛选；每行都保留**原始 JSON** —— 教务哪天改了列名，不用等新版本，界面上当场就能看出真实列名
· 名单为空时会报出**教务返回的顶层字段名**，把「确实没开课」和「教务改了字段名」分得清清楚楚，而不是笼统显示「没有数据」
· **两个域都检测**：本科教务挂着两个域名，现在会**分别探明每个域名上到底是什么**，并如实汇报各自的结论 —— 不合并，因为合并会把「这个域压根没有这套系统」伪装成「两个域都没这门课」
· **抢课时两个域都发**：提交与占位都同时打向两个域，谁先给受理号就用谁的去跟踪；两条受理号都记在任务上、轮询时两边都看 —— 只盯一边的话，抢到课的那条会被当成「还在处理」一直轮询到超时，然后被当成失败重投
· 判定逻辑主域与镜像域**共用同一套**，不会出现「一边认成功、另一边认失败」这种自相矛盾的结果
· **修正一处会让人一登录就撞墙的误判**：上一版把 `bkjw.guet.edu.cn` 当成「同一套系统的正式域」并设为**默认**，这是错的。逐路径实测：`bkjw` 是**另一套系统**（ASP.NET + ExtJS 桌面，登录走统一身份认证 CAS），它的 `/student/**` **全部 404** —— 连静态资源 `eams-ui.js` 都是 404，而静态资源不受鉴权影响，说明这套系统根本没部署在那个域名上。默认域已改回实测能跑通的那个，`bkjw` 作为**候选域名**参与探测
· 据此，「两个域都发」是**先探测再发**：没有这套系统的域不盲发（发过去只会 404，既浪费一次出手机会，又会在结果面上留一条把人引向错误方向的「失败」），并且**把不发的原因说出来** —— 静默跳过比发错更糟，用户会以为两边都在打
· 修正：**渐进式模糊「升级后消失」**。画质档位的缺省值原本是「低」，而渐进模糊遮罩是条件渲染 —— 全新安装没有本地档位记录，于是遮罩被整个跳过，看起来就像「装完少了一块设计」（0.2.5 升到 0.2.7 后正是这个表现）。默认改为「自动」：先给最好的，实测扛不住再退
· 修正：**在线服务填对 API key 仍报 connection error**。App 里的聊天请求是 WebView 侧直接发出的，而网关没有给跨域许可 —— 浏览器只会回一句 `Failed to fetch`，把「密钥不对」「限流」「provider 全冷却」这些**真正有用**的信息全吞掉。网关已补上跨域支持（只对 AI 接口开，且是 Bearer 令牌鉴权、不带 Cookie），并**回显**请求方要的自定义头（OpenAI SDK 会带 `x-stainless-*` 一类，白名单少一项预检就失败，表现同样是「一句 connection error，什么都看不到」）
· 抢课设置里连的是正式域时会**明说**：下面那些实测数字来自测试域，正式域的风控策略未知 —— 测试域压出来的速率不能当作正式域的结论
· 冒烟脚本默认切到测试域，不拿正式教务当靶子（需要时用 `REIN_GUET_SYSTEM=prod` 显式指回）
· 回归锁死：`the_default_domain_is_the_one_that_actually_serves_eams5`（默认域必须是实测能跑通握手的那一个）、`a_domain_without_eams5_is_skipped_with_a_reason`（没有这套系统的域不盲发，且必须写明原因）、`rows_are_found_in_every_envelope_shape_the_school_uses`（信封变了也要认得出来）
## v0.2.7

· **抢课计划不再可能抢错年级**：命中**两个不同课程代码**的查询（最典型是大一/大二双开的同一门体育课：`大学体育1` 000004 / `大学体育3` 000006）在「中一个就够」下**不会排队**，而是停在「要补课程代码」并列出命中哪几门课 —— 引擎不会再把另一个年级的班排进志愿组。「每门课都要」不受影响（那本来就是多门的意思）
· 预览与计划行都会提前说清这件事：写计划时就告诉你「这句同时命中了 2 门课」，补上代码（`羽毛球 000004`）立刻收敛到那一门
· 回归锁死：**写下的课程代码永不放宽** —— 带代码的查询命不中就零命中，绝不会退回按课名匹配（单测 `a_written_course_code_is_never_loosened`、`name_only_query_spanning_two_grades_is_flagged`）
· 修复：服务端管理接口 500（`EROFS .../config.json.tmp-…`）。配置是运行时可改的，而 systemd 的 `ProtectSystem=strict` 只放行了 `/var/lib`，且 config.json 是 `640 root:rein` —— 签发客户端密钥、热更新价目表这类写操作全都会失败。单元与安装脚本都已修（`ReadWritePaths` + 目录 775/文件 664），跑一次 `scripts/release/deploy-server.mjs` 即生效
· 修复：Android **release** 包里的 `usesCleartextTraffic` 是 `false`，而更新源与在线服务按设计走明文 HTTP —— 表现为设备上「连接失败」。已放开并注明迁移到 TLS 时再翻回去
· 新增 `scripts/verify-online.mjs`：一条命令体检在线服务（服务可达 → 管理面可写 → 目录 → 对话 → 流式 → 用量 → 清理临时密钥），把「连不上 / 密钥不对 / provider 冷却」分得清清楚楚
· 新增 `scripts/e2e-online-cost.mjs` 覆盖在线服务计费与账本

## v0.2.5

· 抢课支持「志愿组」：同组课程互为备选（同一门课的多个教学班、或时间冲突的几门），**只会中一个**，中选后同组其余自动取消
· 一次选中多个教学班「多选预定」，**勾选顺序就是志愿序**；批量抽屉里可选「各抢各的」或「排成志愿组」
· 任务单按志愿组渲染：组头写「第 N 志愿在抢」，被压住的成员写「等第 N 志愿」，一眼看出谁在等谁
· 新增「让贤期限」：当前志愿连续满员超过设定时长就把机会让给下一志愿；**0 = 死守**（默认，只有彻底没戏才轮到下一个）
· 窗口监听：App 开着就每分钟问一次「窗口公布了没有」，**即使还没排任何课**；窗口一开跨页面播报，选课页显示监听状态
· 选课页自动刷新：批次 60s、教学班 30s，切回前台立刻补一次 —— 不用再手点刷新按钮
· 抢课节奏设置新增「抢课循环时间轴」：五条泳道按真实比例画出每段延迟，引导线标出参数名与当前值，调哪个哪个点亮
· 放开节流下限（最小间隔可到 10ms ≈ 100 次/秒，实测 80 次/秒无异常），并新增「保守 / 偏快 / 压测档」三档预设；**默认值未变**
· 数字步进器支持**点数值直接输入**：参数区间拉开后，只有 +/- 根本走不到低档
· 修复：已出局的任务仍显示「等第 N 志愿」，看起来像还在排队
· 修复：抢课浮条的「抢到了 / 窗口已开放」播报只在挂载那一刻生效
· **抢课计划**：用一句模糊查询（「高数 张」「体育」「000031」）写下「我想抢什么」，引擎到点自己解析成志愿任务并开抢 —— **提前输入，之后完全不用管**
· 模糊匹配：空格分词、每词都要命中，落在课名 / 课程代码 / 教师 / 上课时间地点上；连续子串优先，退化为**散落子序列**（「高数」命中「高等数学」）
· **指定老师**：把老师名字打全（`张伟`）就是「指定」—— 只抢这位老师的班，名字沾边的另一位老师（`张伟明`）不再进志愿组
· 老师名字打错一个字（`张玮`）不会一门都搜不到：按「姓相同、最多差一个字」**退回模糊匹配**，并且界面明说「这是猜的、请核对姓名」
· 分数相同的教学班之间仍然「有余量的先出手」；分数不同时按匹配度 —— 一个错字不该让引擎先去抢另一位老师的班
· 计划预览：按「此刻教务的名单」列出会抢哪些教学班与出手顺序，并标出命中字段（课程名 / 代码 / 教师精确 / 教师近似）
· 计划可以在**批次还没公布时**就写好：`open-turns` 还是空的时候计划先存着，批次一出现引擎自动解析 + 排队 + 到点开抢
· 计划的两种抢法：「中一个就够」（全部命中互为备选，只中一个）与「每门课都要」（按课程分组，每门课各中一个班）
· 计划行揭示解析结果：会抢哪几个班、谁是第一志愿、谁已满；「重新解析」按最新名单重排，「移除」连它派出去的任务一起取消
· 修复：计划输入框改为提交前清空（失败再把原文放回），不再吃掉等待期间敲下的下一个词
· 修复：`e2e-course-select` 的刷新按钮选择器过期（页面早已是「立即刷新」）
· **AI 抢课救援**：抢课页新增「交给 AI 排查」——一键把现场（引擎故障原文、卡住的每个任务卡在哪一步、计划解析状态、批次窗口）递给 AI，由它判断该重登、该重排、该改节奏，还是教务那边变了
· AI 现在能直接编排抢课：看现场、查名单、排课、重试卡住的任务、暂停恢复、改节奏、直接提交/退课，以及**带会话打任意请求看原始响应**（教务改了接口时唯一能救回来的手段）
· 新增 8 个教务工具，与页面调的是同一批命令：AI 排出来的任务和你自己在页面上排的长得完全一样
· AI 的每一步都记进「课表配置 → AI 操作记录」（最近 20 条，含失败），写操作不弹确认也不怕事后说不清
· **一键导出救援脚本**：把 AI 这一路打过的请求渲染成一份带 Cookie 的 bash 脚本，落成 `.sh` 文件并在知识库留一份可读副本 —— App 被杀掉、手机没电、人在另一台机器上时靠它继续
· 安全边界：带 Cookie / 选课令牌的请求**只允许打教务自己的域名**（跨域直接拒绝，不允许安静降级），外部地址一律不带凭据且过私网拦截；同一条请求 1 分钟内超过 20 次熔断，救援工具不会变成新的故障
· 会话在请求过程中失效会自动重登并重试一次，响应里明确标出「上面是重试后的结果」
· **三类应急各走各的路**：教务**限速**（请求过于频繁 / 429）与教务**服务器出错**（5xx，含连接被重置）时**不退避、不放手** —— 任务立刻回到队首、继续按你设的节奏打，界面上写明「教务在限速（继续按节奏重试）」并把教务原话留着；窗口只有几分钟，退避一次就少几十次机会
· 教务说**参数错误**（400/422、缺少参数、格式不对）时引擎**立刻停手**：重试一万次也是同一个结果，继续撞只是在用错的参数骚扰教务。任务落到「请求被拒」，**现场自动递给 AI**（跳到 AI 页并把现场作为问题发出去，指名道姓说清是哪条请求、教务原话是什么、该手工打一次看原始响应），抢课页也留下「交给 AI 排查 / 重新排队试试」两条出路
· 被限速或 5xx 消耗的提交次数**不计入**你设的提交上限：那是对方的毛病，不该把窗口里的额度耗光
· 修复：未安排池的卡片拖拽在「指针捕获失败」时会整个失效（合成指针、指针已失效等边缘场景）—— 捕获失败改为只影响出界跟踪，拖拽照常
· 修复：课表周视图滚到「今天」那一列时，若目标位置超出可滚范围，浏览器会把它夹在中间，左边留下一条前一天的残影；现在退到最靠右的列边界，列与列严丝合缝
· 修复：`verify.mjs` 把本机架构写成 `x64`（清单里是 `x86_64`），导致「本机永远找不到自己的条目」；改用与 `platform_key()` 逐字对齐的架构名
