# 组件审查：MacroDetailCard.vue

> `src/components/nutrition/MacroDetailCard.vue` · 106 行 · 宏量营养素详解：当日进度数值 + 作用与来源说明

**评级：A**（A=可直接作为范本；B=整体扎实，有应修项；C=存在手感/规范硬伤）

## 总评
纯静态展示卡：无任何交互元素、无动画，因此无按压反馈/hover 隔离/reduced-motion 等义务；颜色经 `colorVar` 引用域色令牌，数值全部 `.num`，排版令牌零魔法值。与 MicrosCard 同构的列表样式保持了营养详情页的一致性。未发现需要修改的问题。

## 问题清单

| # | Before | After | Why | 优先级 |
| --- | --- | --- | --- | --- |
| – | – | – | 未发现违反方法论或令牌规范的条目（无交互、无动效、无魔法值） | – |

## 做得好的
- L34/L37：圆点颜色走 `var(${r.colorVar})` 令牌绑定，数值挂 `.num`，当日进度变化时文字不跳动。
- L67-L68：`.item + .item` 分隔线用 `0.5px var(--line)` 发丝线令牌，iOS 细线质感。
- L10-L20：数据层在 computed 内做 `Math.round` 与缺省兜底（`?? 0 / ?? 'g'`），模板保持纯净。
- 明智地「不动」：说明文案为常驻阅读内容，没有加任何入场 stagger 或 hover 效果——符合「装饰性动画对功能型内容是干扰」的判断。

## 修复建议排序
- **P0**（明显手感缺陷或违反方法论核心）：无。
- **P1**（应修）：无。
- **P2**（打磨项）：无。
