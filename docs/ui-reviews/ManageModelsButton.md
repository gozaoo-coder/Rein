# 组件审查：ManageModelsButton.vue

> `src/components/ai/ManageModelsButton.vue` · 40 行 · 全应用统一的「管理模型」胶囊入口按钮，AI 页与各智能添加弹层共用

**评级：A**（可直接作为范本）

## 总评

40 行的单一职责组件，把小按钮该做对的事全部做对了：全令牌化、aria-label 齐全、按压反馈依赖全局基线而不重复实现、点击立即路由跳转不加任何动画——中频导航入口零动效正是方法论「该不该动」的正确取舍。未发现符合 P0-P2 口径的问题。

## 问题清单

| # | Before | After | Why | 优先级 |
| --- | --- | --- | --- | --- |
| — | — | — | 未发现符合口径的问题 | — |

## 做得好的

- L17 `aria-label="管理模型"` 齐全（文字+图标双保险，读屏语义稳定）。
- L10-13 导航动作零动画：点击即 `router.push`，无多余过渡，响应干脆。
- L25-34 颜色/圆角/字号全部引用令牌（`--surface-2`、`--radius-full`、`--fs-caption`、`--text-3`），无魔法值。
- 原生 button 元素，自动获得 base.css 的 `:active scale(0.96)` 按压反馈，组件内不重复造轮子。
- L20 右箭头用 `--text-3` 弱化处理，主次层级正确。

## 修复建议排序

- **P0**（明显手感缺陷或违反方法论核心）：无。
- **P1**（应修）：无。
- **P2**（打磨项）：无。
